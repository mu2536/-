# 아이디어
## 아이디어명
날씨 기반 코디 추천 어플

## 아이디어 설명
현재 나의 위치의 날씨와 기온 , 습도 , 바람의 세기등을 기반으로 옷을 추천해주는 어플

## 사용 기술
- React
- TailwindCSS

# 요구사항 명세
## 기능적 요구사항
화면구상
화면에 오늘 날씨가 어떤지 그림으로 나타내고 그옆에 온도를 표시한다.
옷 추천 해주는 화면을 만든다.
추위와 더위를 예측하는 그래프를 만든다.

디자인
날씨는 그림으로 나타낸다.
옷 추천 해주는 화면은 옷 그림으로 나타낸다.
추위와 더위를 예측하는 것은 그래프로 나타낸다.

애니메이션
앱을 들어갔을때 자동으로 날씨와 온도를 나타내고 적당한 옷을 추천해주는 화면이 자동으로 뜬다.
화면을 넘기면 더운 옷을 추천해주는 화면과 추운 옷을 추천 해주는 화면을 뜨게한다.
이때 옷을 추천해주는 화면은 옷, 바지 이렇게 구분지어서 나타낸다.

## 화면 설계
/screen.png 파일에 그려놓은 화면과 최대한 비슷하게 만들어줘



---
### impl plan
날씨 코디 어플 웹앱 구현 계획 (Weather-Based Outfit Recommendation App)
주신 AGENTS.md 파일을 읽어보려 했으나, 현재 디스크 상에는 파일 내용이 비어있는(0 byte) 상태입니다. (편집기에서 아직 저장하지 않으셨다면 저장 부탁드립니다!)

하지만 디렉토리 이름과 이전 대화 기록을 바탕으로 **'날씨 기반 옷차림 추천 애플리케이션(웹앱)'**을 제작하시려는 목표를 확인했습니다. 이를 바탕으로 아래와 같이 구현 계획을 제안합니다.

User Review Required
WARNING

현재 AGENTS.md 파일이 디스크 상에 비어 있습니다. 해당 파일에 중요한 기획이나 추가적인 요구사항을 작성하셨다면 파일을 저장한 후 저에게 다시 알려주시기 바랍니다! (혹시 screen.png 스크린샷 이미지 파일을 기반으로 UI를 제작해야 하는 것이라면 알려주세요.)

Open Questions
IMPORTANT

기술 스택: 빠르고 가벼운 React 기반의 Vite 환경을 사용할까요, 아니면 Next.js를 사용할까요? (특별한 선호가 없으시면 Vite + React 조합으로 제안합니다.)
스타일링: Tailwind CSS를 사용하시겠습니까, 아니면 순수 CSS(Vanilla CSS)로 모던한 UI(글래스모피즘 등)를 구성할까요?
날씨 API: 실제 OpenWeather API 등을 연동하시겠습니까, 아니면 우선 데모용 가짜(Mock) 데이터를 사용하여 UI를 먼저 완성할까요?
Proposed Changes
1. 프로젝트 초기화 및 기반 설정 (Foundation)
Vite + React 프로젝트 생성 (npx create-vite@latest ./ --template react)
모던하고 트렌디한 디자인 시스템 구축 (index.css를 통한 전역 변수 설정, 다크모드/라이트모드 지원, 부드러운 애니메이션)
2. 컴포넌트 개발 (Components)
WeatherDashboard: 현재 온도와 날씨 상태를 직관적인 아이콘과 함께 보여주는 메인 대시보드 컴포넌트.
OutfitSwiper: 상의(Top)와 하의(Bottom) 카테고리별로 옷차림 추천을 스와이프(Swipe) 할 수 있는 인터페이스.
ThermalGraph: 시간에 따른 체감 온도(열/추위) 변화 추이를 시각화하는 예측 그래프 (Chart.js 또는 Recharts 활용).
3. 화면 조합 및 스타일링 폴리싱 (Assembly & Polish)
모바일 환경에서도 보기 좋은 반응형 레이아웃 적용.
사용자 경험(UX)을 극대화하기 위한 호버 효과 및 마이크로 애니메이션 추가.
Verification Plan
Automated Tests
로컬 개발 서버(npm run dev)를 실행하여 브라우저에서 렌더링 및 인터랙션이 정상 작동하는지 확인.
브라우저 콘솔 에러가 없는지 점검.
Manual Verification
사용자(USER) 분께서 직접 로컬호스트 주소로 접속하여 추천 화면의 스와이프 동작과 날씨 표시 UI, 그리고 전반적인 디자인 퀄리티를 확인.
위 계획이 마음에 드시거나, AGENTS.md 저장 후 추가 요구사항이 있다면 편하게 말씀해주세요! 승인해주시면 즉시 작업을 시작하겠습니다.